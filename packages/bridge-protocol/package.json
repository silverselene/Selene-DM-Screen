{
  "name": "@workspace/bridge-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared, types-only wire contract for the AI bridge (HTTP+SSE) and the dm-screen AI Chat widget. No runtime code, no dependencies — imported with `import type` and erased at build time.",
  "exports": {
    ".": {
      "workspace": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
